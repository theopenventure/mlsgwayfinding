export default function ServiceTag({ children }) {
  return (
    <span className="inline-flex items-center px-5 py-3 rounded-full border border-stroke bg-transparent text-heading text-sm font-normal">
      {children}
    </span>
  )
}
